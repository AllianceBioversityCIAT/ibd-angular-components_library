import { SearchAndReplaceAsStrongPipe } from './search-and-replace-as-strong.pipe';

describe('SearchAndReplaceAsStrongPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAndReplaceAsStrongPipe();
    expect(pipe).toBeTruthy();
  });
});
